# H100 NVL 캠페인 — G-0 / §4 관문 기록 (2026-09-02)

**측정 중에는 이 문서를 고치지 않는다.** 관측한 것은 아래 "캠페인 중 관측"
에만 덧붙인다. 조건 확정은 G-4/G-7 통과 뒤에 이 문서 위쪽 표로 굳힌다.

> ## ⛔ 아직 전수를 켤 수 없다 — 미해결 셋
>
> | | 무엇 | 누가 풀 수 있나 |
> |---|---|---|
> | B-1 | **클럭 고정에 root 가 필요하다** (`nvidia-smi -lgc`). 이 계정은 sudo 없음 | 관리자 |
> | B-2 | **컨테이너 런타임이 없다** — docker/podman/apptainer 전부 없음. 5090 과 같은 이미지(`cu133`)를 쓸 수 없다 | 관리자 또는 조건 변경 승인 |
> | B-3 | `peak_tflops_f16` 을 **mma.sync 도달치**로 둘 것인가 **데이터시트 wgmma 피크**로 둘 것인가 (아래 4 절) | 캠페인 설계 판단 |

---

## 1. 환경 (관측값)

| | 값 | 비고 |
|---|---|---|
| GPU | **NVIDIA H100 NVL** `GPU-4c87eb8a-3b54-1f81-7524-8505ce8d8754` | ★ SXM 도 PCIe 도 아닌 **제 3 변종** |
| arch | `sm_90` (Hopper) | 이 저장소의 첫 데이터센터 GPU |
| SM / smem(optin) / regs | **132** / **232,448 B (227 KB)** / 65,536 | ★ smem 이 A6000·5090 의 **2.3 배** |
| L2 | 60 MB | A6000 6 MB, 5090 96 MB |
| 메모리 | 6144-bit HBM3 @ **2619 MHz** (지원 클럭 **단 한 개**) | ★ P-state 로 메모리 클럭이 흔들리지 않는다 |
| 최대 SM 클럭 | 1785 MHz (지원 격자 15 MHz 간격, 195 단) | 유휴 345 MHz |
| 전력 | 현재 상한 **400 W** (기본 310, 최대 400) | |
| MIG | **Disabled** | 전수 측정 가능 |
| ECC | **Enabled** (끄지 않았다) | 실효 대역폭이 계산값보다 낮다 — G-3 에서 실측할 것 |
| 커널모드 드라이버 | **570.158.01** (CUDA 12.8) | |
| 유저모드 libcuda | **570.158.01** (호스트, forward-compat **없음**) | 컨테이너가 없으므로 이미지가 고정해 주지 않는다 |
| nvcc | **12.8.93** (호스트) | ★ 5090/A6000-컨테이너는 13.3.73 |
| CUTLASS | **v4.7.0 `dcf215af`** (`/data/yhlee/workspace/cutlass`) | 5090 과 같은 커밋 |
| manifest 태그 | `kerneltab:cu128-14907dd9` | 호스트 계산값 |
| 공유 여부 | ★ **공유 서버** (로그인 23 세션, 다른 사용자 존재). GPU 프로세스는 현재 0 | 클럭 고정·장시간 점유는 협의 필요 |

> ⚠️ **5090 과 바뀌는 변수가 둘이다** — GPU 와 **툴체인(CUDA 13.3 → 12.8,
> forward-compat 없음)**. `docs/next_campaign.md` 가 12.4 이미지를 만들지
> 않기로 한 이유("두 변수가 동시에 바뀌면 비교가 성립하지 않는다")가 여기
> 그대로 걸린다. 전이 실험에서 **GPU 차이와 툴체인 차이를 분리할 수 없다.**
> 컨테이너를 쓸 수 있게 되면 그쪽이 낫다.

## 2. ★ §4 첫 관문 — `sm_90` 에서 CUTLASS 2.x 가 성립하는가

**결론: 성립한다. 다섯 항목 전부 통과.** `backends/__init__.py` 의
`get_backend` 와 `CutlassV2Backend.arch_family` 에 `sm_90` 을 넣었다.
**커널 생성부(`emit_cpp` / `TB_TILES` / `STAGES` / `is_valid_*`)는 무수정이다.**

| # | 항목 | 결과 |
|---|---|---|
| 1 | nvcc 가 sm_90 을 아는가 | ✅ `--list-gpu-arch` 에 `compute_90` |
| 2 | 2.x 가 sm_90 에서 빌드되는가 | ✅ 프로브 25/25 + **무작위 표본 240/240** (100 %). `cuobjdump -lelf` 로 `*.sm_90a.cubin` 확인 |
| 3 | **참조값 대조** | ✅ 실행된 24 개 전부 `max_rel_error = 0.000e+00` (cuBLAS `COMPUTE_32F` 대조). 나머지 1 개는 `launch_infeasible`(regs 137 × threads 512 = 70,144 > 65,536) — 저장소의 `kernels.launchable()` 이 이미 거르는 경로다 |
| 3b | split-K serial/parallel | ✅ 두 커널 × 축 10 종 × 2 모드에서 **요청 == 실제 `grid.z` 32/32 일치**, 참조 불일치 0. serial 의 `1e-3` 대 오차는 fp16 부분합(결정 6) |
| 4 | `pipeline_kind` 의미 유지 | ✅ `stages=2` → LDGSTS **0** (4/4), `stages≥3` → 전부 > 0 (18~200). A6000/4090/5090 과 같은 갈림 |
| 5 | 축 중 몇 개가 유효한가 | 아래 3 절 |

검증 범위: alignment `a888 / a448 / a228 / a118 / a884 / a882` 전부,
stages 2~8 전부, swizzle `identity{1,8}` + 직접 구현한 `horizontal`,
tile_k 32/64 와 warp_k 분할, 그리고 **H100 에서만 유효해진 큰 smem 커널**
(smem 102 KB ~ 224 KB) 5 개.

### `env_hash` 는 어떻게 바뀌는가 (§4 마지막 확인)

* `axis_space_hash` = `401555c4…` — **바뀌지 않는다.** 예상대로다. 축 목록은
  모듈 상수(`TB_TILES`/`STAGES`/`SPLIT_K`…)에서 나오고 arch 와 무관하다.
* `shape_grid_hash` 는 바뀐다 (`sm_count` 132 → 층 C 가 달라져 **형상 68 개**).
  A6000 65 / 4090 68 / 5090 66 / **H100 68**.
* 실제 탐색 공간이 2 배가 된 것(아래)은 `hardware.smem_per_block` 가
  `env_hash` 의 `hardware` 에 들어 있어 **간접적으로** 덮인다.
  ⚠️ 축 목록이 같아도 **유효 공간이 GPU 마다 다르다**는 것이 이번에 처음
  드러났다. `axis_space_hash` 만 보고 "같은 공간" 이라고 하면 틀린다.

## 3. ★ 탐색 공간이 5090 의 2 배다 — smem 227 KB 때문

| | A6000 / 5090 | **H100 NVL** |
|---|---:|---:|
| smem(optin) | 101,376 B | **232,448 B** |
| 열거 커널 | 7,230 | **13,975** |
| 그중 H100 에서만 유효 | — | **6,745** |
| 런치 가능 (표본 240 추정 82.9 %) | 5090 6,305 | **약 11,580** |
| 형상 | 66 | **68** |
| (형상 × 커널 × 런타임) 작업 | 1,262,825 | **2,906,225** (열거 기준) |

같은 코드에 smem 만 101,376 을 넣으면 정확히 **7,230** 이 나온다 — 즉 차이의
원인은 전부 `smem_capacity` 제약이다. 새로 열린 6,745 개는 `stages` 5~8 과
`tile_k=64` 쪽에 몰려 있다.

**일정 영향**: 5090 의 작업당 67.9 ms 를 그대로 쓰면 2.9 M × 67.9 ms ≈ **55 시간**
(런치 가능 필터를 적용하면 약 45 시간). H100 이 빠르지만 시간 예산
프로토콜에서는 GPU 속도가 캠페인 길이를 거의 바꾸지 않는다(결정 20).
**세그먼트 수가 늘어 재시작 횟수도 늘어난다 — G-4/G-5 에서 다시 계산할 것.**

## 4. ★ FP32 누산 반감 실측 — **없다**. 그런데 다른 문제가 나왔다

`tools/peak_mma_probe.cu` 를 **새로 작성했다** (§6-4 는 4090 캠페인에서
만들었다고 하지만 저장소에 없다). 메모리 접근 없는 `mma.sync.aligned.m16n8k16`
루프 + `clock64()`.

```
FP32 누산   1324.0 FMA/clk/SM   554.7 TFLOP/s @ 관측 1587 MHz
FP16 누산   1365.1 FMA/clk/SM   569.1 TFLOP/s @ 관측 1587 MHz
비율 1.031  ->  ★ 반감 없음 (A6000 계열, GeForce Ada/Blackwell 과 다르다)
역산 클럭 1587 / 1587 MHz 일치  ->  측정 건전
warp 4~24 x 독립누산기 4~16 스윕에서 1323.4~1324.0 로 포화 (포화 확인)
```

### ⚠️ 새로 드러난 것 — mma.sync 로는 데이터시트 피크에 못 간다

| 경로 | 실측 | 1785 MHz 환산 |
|---|---:|---:|
| **mma.sync** (= 우리가 재는 CUTLASS 2.x) | 554.7 TFLOP/s @1587 | **623.9** |
| cuBLAS `COMPUTE_32F` 4096³ (wgmma 경로) | 717.4 TFLOP/s @~1590 | ~806 |
| 데이터시트 (dense, FP32 누산) | — | ~835 |

Hopper 의 최고 처리량은 **wgmma(warpgroup async, 3.x API)** 에서만 나온다.
`mma.sync` 는 서브파티션당 HMMA.16816 을 6 사이클에 하나씩 내는 것이 상한이고
(4 × 2048 / 6 = 1365.3 — 실측 1365.1 과 일치), 그것이 위 표의 1.29 배 격차다.

**A6000/4090/5090 에서는 이 구분이 없었다** (mma.sync 가 곧 하드웨어 피크였다).
H100 에서 처음으로 **"이 표의 config 공간이 GPU 의 피크에 도달할 수 없다"**
가 됐다. `peak_tflops_f16` 은 ridge point 의 분자이자 `frac_of_peak` 의
분모이므로 선택이 결과의 의미를 바꾼다.

```
mma.sync 623.9 를 쓰면    ridge = 623.9 / 4022.8 = 155.1 FLOP/byte
데이터시트 835 를 쓰면    ridge = 207.6            (A6000 159.1 / 5090 117.9)
```

**현재 `known.json` 에는 mma.sync 값(623.9)을 넣었다.** 근거: `frac_of_peak`
와 `is_memory_bound` 는 **실제로 도달 가능한 값** 기준이어야 하고, 이
하네스는 2.x 만 잰다. 도달 불가능한 피크를 쓰면 전 형상의 `frac_of_peak` 가
구조적으로 25 % 깎이고 bound 분류가 통째로 이동한다.
**되돌리려면 `hwspec/known.json` 한 줄이다 — phase0 전에 결정할 것.**

## 5. `known.json` 항목 (추가 완료, 스키마 통과)

```
peak_tflops_f16            623.9        132 SM x 1324 FMA/clk/SM x 2 x 1.785 GHz
peak_tflops_f16_at_mhz     1785
bandwidth_gbps_at_mem_mhz  2619
bandwidth_gbps_spec        4022.78      6144-bit x 2 x 2619 MHz (데이터시트 표기 3.9 TB/s)
mem_bus_bits               6144
```

`tests/test_hwspec.py` 17 항목 통과 (대역폭 교차검증 포함), 전체 테스트
**375 passed / 3 skipped**.

⚠️ **ECC 가 켜져 있으므로 실효 대역폭은 4022.78 보다 낮다.** 이 하네스는
대역폭 계산 경로를 `버스폭 x 2 x 관측클럭` 하나로 고정했으므로(의도된 설계),
ECC 오버헤드가 반영되지 않는다. **G-3 에서 실측 대역폭을 따로 재서 기록할 것.**

## 6. 저장소에 없는데 절차서가 있다고 하는 것들

| 파일 | 상태 |
|---|---|
| `tools/peak_mma_probe.cu` | ❌ 없었다 → **이번에 작성** |
| `tools/tick_probe.cu`, `tools/tick_report.py` | ❌ 없다 — G-3 눈금 측정에 필요 |
| `tools/sol_screen.py` | ❌ 없다 — G-3 `below_launch_overhead` 사전 계산에 필요 |
| `docs/principles.md` | ❌ 없다 (`decisions.md` 에 흡수된 것으로 보인다) |
| `known.json` 의 `fma_per_clk_per_sm` / `sm_count_spec` 필수 키 | 실제 스키마(`tests/test_hwspec.py`)에는 없다. 6 키만 필수 |

## 7. G-0b 축 덮개 — **exit 0**, 3.x 추천 없음 (2026-09-02)

```
추천 612 건 / 68 형상 (nvidia-matmul-heuristics, target=CUTLASS, 프리셋 H100_NVL)
cluster : 612/612 이 (1,1)          instr : 612/612 이 (16,8,16)   ★ 3.x 아님
공간 밖 : 17/612 = 2.8 %  전부 stages=1 (근거 있음 — 수치가 틀리는 값이다)
```

A6000 6.0 % / 5090 17.8 %→2.8 % / 4090 2.8 % / **H100 2.8 %**. 축 구멍 없음.

> ⚠️ `GPU_PRESETS` 에 `h100 nvl` 을 넣기 전에는 `h100` 이 먼저 걸려 **H100_SXM
> 프리셋**이 조용히 쓰였다. 변종마다 SM 수·메모리가 다르므로 그대로 뒀으면
> 다른 GPU 의 추천으로 축 덮개를 판정할 뻔했다.

### ★ 벤더 추천의 62.6 % 가 A6000/5090 공간 밖이다

같은 추출물에서 각 추천의 smem 을 계산해 봤다.

| | 건수 | 비율 |
|---|---:|---:|
| smem > 101,376 B (A6000/5090 이 못 담는 config) | **383 / 612** | **62.6 %** |
| ★ 형상별 **top-1** 추천이 그런 config 인 경우 | **29 / 68** | **42.6 %** |

즉 `stages` 7~8 과 큰 타일이 H100 에서 벤더의 주력 추천이다. 이 영역을 안
재면 **벤더 baseline 을 채점할 수 없다** — 벤더가 1 순위로 고른 config 가
표에 없는 형상이 68 개 중 29 개가 된다. `split_k` 5·7 구멍과 같은 논리다.

## 8. G-3 잠정 (클럭 미고정 상태의 값 — 고정 후 다시 잰다)

### 눈금 = **32 ns**

`tools/tick_probe.cu` + `tools/tick_report.py` 를 새로 썼다. 목표 길이
2 us ~ 2 ms 여섯 구간 × 800 회.

```
전 구간에서 32 ns, 설명력 100 %
16 ns 후보는 설명력 100 % 이지만 ★ 홀수 배수가 0.0 % -> 기각
                                  (32 의 절반이라 짝수 배수만 관측된다)
64 ns 이상은 설명력 50 % 이하
A6000 1024 / 4090 32 / 5090 16 / ★ H100 32 ns
```

### SOL 하한 — ⛔ 여섯 형상이 문턱 아래, 그중 넷이 층 A 다

`below_launch_overhead` 문턱 = 3 × 3.584 us = **10.752 us**
(5090 은 12.288 us. H100 의 런치 오버헤드가 더 작다).

| 형상 | SOL @1785 | 문턱 대비 | @1400 가정 | 지배 | 층 |
|---|---:|---:|---:|---|---|
| `1x4096x4096` | 8.35 us | **77.6 %** | 77.6 % | memory | A |
| `8x4096x4096` | 8.37 | **77.9 %** | 77.9 % | memory | A |
| `32x4096x4096` | 8.47 | **78.8 %** | 78.8 % | memory | A |
| `128x4096x4096` | 8.86 | **82.4 %** | 82.4 % | memory | A·C |
| `2048x4096x256` | 6.88 | **64.0 %** | 81.6 % | compute | B |
| `4096x4096x128` | 8.86 | **82.4 %** | 82.4 % | memory | B |
| `256x4096x4096` | 13.77 | 128.1 % | 163.2 % | compute | A·C |
| `4096x4096x256` | 13.77 | 128.1 % | 163.2 % | compute | B |

**★ 메모리 바운드인 것은 SM 클럭을 고정해도 안 움직인다** — 메모리 클럭이
2619 하나뿐이기 때문이다. 즉 넷은 **어떤 클럭 조건에서도 문턱 아래**다.

이 구간이 가장 나쁜 자리다 (문턱의 64~82 %): 느린 config 는 살고 **빠른
config 만 잘린다.** 결측이면 차라리 낫다.

원인은 H100 의 대역폭이다. `M<=128 x 4096 x 4096` 은 B(33.5 MB)를 읽는
시간이 지배하는데 4.0 TB/s 면 8.4 us 다 — 런치 세 번보다 짧다. 같은 형상이
5090 에서는 19 us(문턱의 155 %)였다.

앵커(`DRIFT_SHAPES` 2048³/4096³)는 문턱의 256 % / 2049 % 로 **안전하다.**

> 층 A 의 다른 세 (N,K) 열은 M=1 에서도 209~233 % 로 살아남는다. 문제는
> **`(N,K) = (4096,4096)` 열 하나**다.

## 9. 컨테이너 — rootless docker 가 된다

```
docker 29.7.2 rootless  (uidmap/subuid/linger/user-systemd 전제조건 전부 충족)
nvidia-container-toolkit 설치돼 있고 no-cgroups = true 도 이미 켜져 있다
apparmor_restrict_unprivileged_userns = 0 (Ubuntu 24.04 인데 제한이 꺼져 있다)
```

**★ forward-compat 이 얹힌다** — 호스트 커널모드 570.158.01(CUDA 12.8) 위에서

```
로드된 libcuda : /usr/local/cuda-13.3/compat/libcuda.so.1 -> 610.43.02
cuDriverGetVersion : 13030   nvcc : 13.3.73   device : H100 NVL sm_90
```

**5090·4090 과 같은 610.43.02 다.** 즉 컨테이너로 가면 CUDA·CUTLASS·유저모드
드라이버가 앞 캠페인과 완전히 같아지고, **바뀌는 변수는 GPU 와 커널모드
드라이버 둘뿐**이 된다.

> ⚠️ 첫 이미지 빌드는 **실패했다.** `tests/test_axis_coverage.py` 가
> `docs/baselines/vendor_*.json` 을 여는데 이미지가 `docs/` 를 안 담았다.
> 그 테스트는 2026-09-01 에 추가됐고 그 뒤로 이미지를 다시 빌드한 적이
> 없었다 — **호스트에서는 375 passed 라 이미지를 만들어 봐야만 드러난다.**
> `COPY docs/baselines/` 를 넣어 고쳤다.

## 10. 컨테이너 `detect` 실측 (2026-09-02, 클럭 미고정 잠정)

```
이미지        kerneltab:cu133-233e21c0
env_hash      3b765cb6...        ★ 잠정 (clock_locked=false). 고정 후 다시 뽑는다
셔플 시드     20260902 (--seed 로 고정)
CUTLASS 예제  ampere_gemm_universal_streamk  5 passed / 0 failed, best 401.6 TFLOP/s
```

| | 호스트 CUDA 12.8 | **컨테이너 13.3 + compat** |
|---|---:|---:|
| `launch_async_grid` | 3.289 us | **3.154 us** |
| `launch_bracketed_grid` (기준값) | 3.584 us | **3.424 us** |
| `below_launch_overhead` 문턱 | 10.752 us | **10.272 us** |
| ridge point (실효) | 155.1 | 155.1 |

문턱이 더 낮아졌지만 고친 그리드의 최소 여유가 **13.77 us = 134 %** 라
그대로 안전하다. 5090 의 문턱은 12.288 us 였다.

## 11. 커널 빌드 — `static_assert: Too many predicates.`

전체 13,975 개 빌드 중(진행 중) 실패가 **한 종류**로만 나온다.

```
sm90_tb64x256x64_w64x64x64_st2_swid{1,2,4}_a118 ...
  -> static_assert: Too many predicates.
```

`a118`(alignment 1) + 큰 타일(64x256x64) 조합이다. CUTLASS 2.x
`PredicatedTileAccessIterator` 의 컴파일 시점 제약이고, alignment 가 작을수록
접근 횟수가 늘어 predicate 수가 상한을 넘는다.

**결측이 아니라 `build_fail` 행으로 남는다** (원칙대로). 빌드가 끝나면
`validate_constraints.py` 가 "모델링하지 않은 실패" 로 집계하므로, 거기서
FN/FP 를 보고 `explain_kernel` 에 세 번째 컴파일 제약으로 넣을지 정한다 —
`decisions.md` 4 번이 같은 방식으로 두 개를 추가한 자리다.

⚠️ 넣더라도 **성능 필터가 아니라 컴파일 가능성 판정**이어야 하고, 실측
전수와 대조해 오탐 0 / 미탐 0 을 확인한 뒤에만 필터로 쓴다.

## 12. ⛔ G-1 실부하 관찰 — **전력 상한이 클럭을 무너뜨린다**

`rehearse --all` (실제 캠페인 경로) 로 부하를 걸고 2 초 간격으로 관찰했다.
합성 soak 이 아니라 실제 경로를 쓴 이유는 5090 교훈이다 (20 분 soak
294~389 W vs 실제 캠페인 중앙 434 W).

```
독립 샘플러 204 표본 (7.5 분, 부하 구간만)
  SM 클럭  min 1095  p1 1200  p5 1335  p10 1455  중앙 1770  max 1785
  전력     230~406 W  중앙 355 W       (상한 400 W, 기본값 310)
  온도     44~66 C    ★ 온도는 원인이 아니다
  sw_power_cap 플래그  155/204 = 76 %

하네스 텔레메트리 561 표본 (1 초 간격, 9 분)
  sm_clock  min ★765  max 1785  mean 1701.3
  power     max 408.4 W        sw_power_cap 429 초
```

| 기준 | 그 아래로 내려간 표본 |
|---|---:|
| 1785 미만 | **77.0 %** |
| 1770 미만 | 40.7 % |
| 1650 미만 | 22.1 % |
| 1500 미만 | 14.2 % |
| 1350 미만 | 5.4 % |

5090 에서는 `sw_power_cap` 플래그가 **신호가 아니었다** (플래그가 뜬 샘플이
전부 목표 클럭이었다). 그래서 클럭 분포로 판정했고, **H100 에서는 플래그와
분포가 일치한다** — 전력 상한이 실제로 물린다.

### 하네스가 스스로 중단했다 — 감시가 설계대로 동작했다

```
!! 드리프트 31.35 % (0.1987 vs 이동기준 0.1513 ms) 연속 3 회 -> 중단
드리프트 프로브 5 회: min 0.9857  max 1.8680 ms  변동폭 73.35 %
```

★ 이것은 모듈 누적 드리프트가 **아니다.** 클럭이 765~1785 를 오간 결과다.
`clock_locked=False` 라 점검 주기가 180 초로 짧아져 있었고, 그 감시가
"조건이 변했다" 로 잡아냈다. `results.jsonl` 은 그대로 남는다.

> ⛔ **클럭 고정 전에는 G-4 를 포함해 어떤 게이트도 판정할 수 없다.**
> 미고정으로 전수를 켜면 A6000 이 226,211 행을 버린 것과 같은 결과가 된다.

### 제안 클럭 — 1500 MHz (1차), 1350 / 1200 (예비)

```
1500 / 1785 = 84 %        A6000 75 % · 5090 82 % 와 같은 수준의 디레이팅
예상 전력 240~300 W       (상한 400 대비 여유 25 % 이상)
실효 피크 524.3 TFLOP/s   = 623.9 x 1500/1785
ridge point 130.3         A6000 159.1 / 5090 117.9 사이
메모리 2619 (지원값 하나뿐이라 -lmc 는 형식적)
```

⚠️ 관측된 765/1095 MHz 는 **부스트가 1785 를 시도하다 전력에 걸려 떨어진
값**이지 고정 클럭의 하한이 아니다. 낮은 클럭으로 고정하면 전압이 함께
내려가 전력이 크게 준다. 고정 후 `verify_clock_lock --minutes 20` +
`rehearse` 로 **부하 중 유지 여부**를 실측해 확정한다.

```bash
sudo nvidia-smi -i 0 -pm 1
sudo nvidia-smi -i 0 -lgc 1500,1500
sudo nvidia-smi -i 0 -lmc 2619
# 측정 종료 후 반드시:  sudo nvidia-smi -i 0 -rgc && sudo nvidia-smi -i 0 -rmc
```

## 13. 게이트 진행표

| | 게이트 | 상태 |
|---|---|---|
| §4 | `sm_90` 관문 | ✅ 통과 (참조 대조 24/24, split-K 32/32, LDGSTS 갈림 동일) |
| G-0 | 이미지·환경 | ✅ `kerneltab:cu133-233e21c0`, forward-compat 610.43.02 |
| G-0b | 축 덮개 | ✅ exit 0 (공간 밖 2.8 %, 3.x 추천 0) |
| **G-1** | **클럭 고정** | ⛔ **막힘 — root 필요** |
| G-2 | P-state·메모리 | 메모리 지원값 2619 하나뿐. 클럭 고정과 함께 확인 |
| G-3 | 피크·ridge·눈금 | 부분 완료 — 눈금 32 ns, 대역폭 실측 3701 GB/s, SOL 0 건 |
| G-4 | 드리프트 3 값 | 대기 (클럭 고정 후) |
| G-5~G-7 | 세그먼트·리허설·재현성 | 대기 |

### 클럭 고정 전에 확정한 것

```
커널 13,955 빌드 (성공 13,955 / 제약으로 열거 제외 20)
정확성 a888 2,125/2,125 정상
smem 40/40 일치 · split-K 3 형상 이상 없음
교차검증 4 종 전부 불일치 0/13,955
작업 수 ★ 2,245,630 (런치 가능 필터 적용 실측)
미고정 처리량 약 1,000 작업/분 -> 약 37 시간
```

## 캠페인 중 관측

(전수 시작 후 여기에 덧붙인다)
