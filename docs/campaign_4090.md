# RTX 4090 캠페인 — 조건 기록 (진행 중)

> **앞선 4090 시도는 측정 문제가 아니라 서버를 더 쓸 수 없어 중단됐다.**
> 새 호스트에서 게이트부터 다시 한다. **앞선 시도의 데이터는 이 저장소에
> 없다** — 커밋도 번들도 남아 있지 않다. 그래서 섞일 표가 없고, 앞선 값은
> "그 시도의 보고(데이터 없음)" 로만 인용한다.

측정 중에는 이 문서를 고치지 않는다. 관측한 것은 "캠페인 중 관측" 에만 덧붙인다.

---

## 조건

| | 값 | 근거 |
|---|---|---|
| 이미지 | `kerneltab:cu133-*` | `docker/build.sh` (이미지 안에서 태그 계산) |
| 베이스 | `nvidia/cuda@sha256:c13e0118...` (13.3.1-devel-ubuntu22.04) | 5090 과 **같은 정의** |
| nvcc / CUTLASS | **13.3.73** / `dcf215af` (v4.7.0) | 5090 과 동일 |
| GPU | RTX 4090 `GPU-bae66527-65c9-2fd9-9fa3-3c88fc3e6d67` | sm_89, **128 SM**, L2 72 MiB, VRAM 24 GB |
| | ★ **4090D 가 아니다** (4090D 는 114 SM) | `peak_derivation.sm_count` 대조로 강제 |
| **커널 모드 드라이버** | **580.173.02** | ★ 아래 참조 |
| 유저 모드 libcuda | **610.43.02** (이미지 compat) — A6000/5090 과 동일 | `cuDriverGetVersion = 13030` |
| `NVIDIA_DISABLE_REQUIRE=1` | **필수** | 호스트가 보고하는 CUDA(13.0) < 이미지(13.3) |
| 호스트 | KVM 게스트, 30 vCPU, 98 GB RAM | |
| **SM 클럭 고정** | 요청 **2400** → 실측 **2400** (스냅 없음) | 지원 클럭 15 MHz 격자 |
| **메모리 클럭** | 요청 10501 → ★ 부하 중 실측 **10251** (P2) | `locked_mem_mhz` 는 실측값 |
| 실효 피크 | **157.33 TFLOP/s** (165.2 × 2400/2520) | `peak_derivation` 128×256×2×2.52 GHz |
| 실효 대역폭 | **984.1 GB/s** (384bit × 2 × 10251) | API 유도 |
| **ridge point** | **159.9** FLOP/byte | A6000 159.1 / 5090 117.9 |
| 형상 | **64** | H100 이 층 A/B/C 를 고친 뒤 (68 → 64) |
| 커널 | 열거 **7,210** = 빌드 성공 **7,210** / 7.9 GB | `--align all` + `predicate_count` 제약 |
| **`env_hash`** | **`90728ca8`** (정의 4) | 재개 키·격리 경계 |
| 셔플 시드 | 1340264798 | |
| `split_k` 축 | 10 종 `[1,2,3,4,5,6,7,8,12,16]` | 5090 에서 넓힌 축. 4090 에서도 구멍 0 |

### ★ 드라이버가 앞선 시도(580.95.05)와 다르다 — 손실이 아니다

게이트 중 `apt-get install nvidia-container-toolkit` 이 드라이버 스택을 함께
올렸다 (580.95.05 → 580.173.02). 배포판이 옛 버전을 교체해 되돌릴 수 없고,
커널 모듈을 재로드해 **양쪽을 580.173.02 로 일치**시켰다.

**맞출 이유가 없다.** `env_hash` 의 목적은 *다른 조건을 한 표에 섞지 마라*
이지 *앞선 캠페인과 같게 하라* 가 아니다. 그리고 앞선 4090 데이터가 없으므로
섞일 표 자체가 없다. **이것이 이 캠페인의 조건이고, 기록하면 끝난다.**

절차는 `docs/new_environment_checklist.md` G-0 에 넣었다 — 게이트 중에는
무해하고 **측정 중에는 치명적**이다.

---

## 게이트 통과 기록

| 게이트 | 결과 |
|---|---|
| G-0 이미지·환경 | 통과. sm_89 `--list-gpu-arch` 확인, CUTLASS 2.x deprecated 아님, 예제 5/5 |
| | ⛔ `d8bfdfd` 이후 이미지가 **빌드되지 않았다** — `docs/baselines` 미포함. `5d428e3` 으로 수정 |
| G-0b 축 덮개 | **exit 0**. 공간 밖 17/611 = 2.8 %, 전부 근거 있는 `stages=1`. 3.x 추천 0건 |
| G-1/G-2 클럭 | **통과**. 240/240 목표 유지, 최대 이탈 **0.00 %**, 스로틀 `gpu_idle` 만 |
| G-3 피크·ridge·눈금 | **통과**. 실측 피크가 예측의 99.6 %, 눈금 32 ns (홀수배수 검사 통과), SOL 최소 여유 592 % |
| **G-4 드리프트** | **통과**. 문턱 [1,498, 1,585] (폭 87 < STEP 100), 기울기 133.8~135.5 us/1k, 세그먼트 **634** |
| G-5 세그먼트 | (대기) |
| G-6 리허설 | (대기) |
| G-7 재현성 | (대기) |

### G-1/G-2 — 고정값을 2400 으로 정한 이유

미고정 실부하(`rehearse` 1,920 작업, 2.2 분)에서:

```
pstate   P2      SM 중앙 2745  최소 2565  최대 2760
메모리   10251 MHz (P0 는 10501)      전력 중앙 334 W / 최대 381 W (상한 450)
온도     41 -> 75 °C (2.2 분 만에)
```

| 후보 | 관측 하한 2565 대비 | 실효 피크 | ridge |
|---:|---:|---:|---:|
| 2520 (정격 부스트) | −1.8 % | 165.2 | 167.9 |
| **2400** | **−6.4 %** | **157.33** | **159.9** |
| 2250 | −12.3 % | 147.5 | 149.9 |

**2520 은 여유가 1.8 % 뿐이다.** 관찰 창이 2.2 분인데 그 사이 온도가 이미
34 °C 올랐다. 20 시간 캠페인은 더 뜨겁고 더 무거운 구간이 있다 — 5090 이
"20분 soak 294~389 W vs 실제 캠페인 중앙 434 W" 로 밟은 자리다.

**낮추는 비용은 사실상 0 이다.** 시간 예산 프로토콜이라 작업당 GPU 시간
상한이 클럭과 무관하게 45 ms 다 (`decisions.md` 20). 캠페인 길이는 작업 수가
정한다.

고정 후 4 분 실부하 검증:

```
clocks.sm    min=max=mean=median 2400 MHz,  목표 이상 유지 240/240 = 100.0%
clocks.mem   median 10251 (부하 중 P2)
power        최대 285.8 W / 450 W = 64%
temp         45 -> 70 °C
throttle     gpu_idle 1s 만.  sw_power_cap 0.0%
time_ms      첫 1/4 2.9071 -> 마지막 1/4 2.9087 = +0.05%,  전체 변동폭 0.74%
```

### ★ `-lmc` 를 걸어도 부하 중에는 P2 로 내려간다

`SUPPORTED_CLOCKS` 는 메모리를 **10501 하나만** 노출하는데, 컴퓨트 워크로드는
P2 로 동작해 **10251 MHz** 다. 고정의 효과는 "유휴 405 ↔ 부하 10251" 이
"유휴 10501 ↔ 부하 10251" 로 바뀌는 것이다. A6000(요청 8001 / 실측 7601)과
같은 현상이고 폭은 훨씬 작다 (97.6 %).

`locked_mem_mhz` 에는 **부하 중 실측 10251** 을 넣는다. 이 보정을 빼면
ridge point 의 분모가 2.4 % 틀린다.

### ridge 가 A6000 과 거의 같다 — `bound_flipped` 를 실제로 셌다

```
A6000   116.1 / 729.7   = 159.1
★ 4090  157.333 / 984.1 = 159.9      (2400 MHz 고정)
5090    208.194 / 1766.5 = 117.9
```

`common_shapes_only` 로 공통 형상만 남기고 각 캠페인의 **실효** ridge 로
`is_memory_bound` 를 계산해 뒤집히는 개수를 셌다:

| 쌍 | 공통 형상 | 메모리 바운드 | **뒤집힘** |
|---|---:|---|---:|
| A6000 (159.1) ↔ **4090** (159.9) | 61 | 13 → 13 | **0** |
| 4090 (159.9) ↔ 5090 (117.9) | 60 | 13 → 9 | **4** |
| A6000 (159.1) ↔ 5090 (117.9) | 60 | 13 → 9 | **4** |

뒤집히는 넷은 두 쌍에서 같은 형상이고 산술 강도가 두 ridge 사이(117.9 ~
159.9)에 있다:

```
128x4096x11008   AI 122.7        128x12288x4096   AI 122.9
128x11008x4096   AI 122.7        8192x4096x128    AI 122.3
```

**소비 쪽에 대한 함의:** A6000↔4090 전이 실험에서는 `is_memory_bound` 를
규칙 입력으로 써도 그 축 때문에 손실이 생기지 않는다. **5090 이 끼면 넷을
따로 세고 손실을 분해해야 한다** — 규칙이 나빠서인지 bound 분류가
뒤집혀서인지 구분되지 않는다.

### G-3 — 실측

#### 실효 피크: `mma.sync` 로 FP32 누산 반감을 확정했다

```
FP32 누산  156.73 TFLOP/s   -> 128 SM x 2 flop x 2.4 GHz 로 역산 255.1 FMA/clk/SM  -> 256
FP16 누산  309.57 TFLOP/s   ->                              역산 503.9            -> 512
비율 1.9752
```

**`known.json` 의 `fma_f16_per_clk_per_sm = 256` 이 실측으로 확정됐다.**
이 항목의 `source` 산문은 2026-09-02 까지 512 로 적혀 있어 값(165.2)의 2 배를
내고 있었다 — 값이 옳고 산문이 틀렸다. 이제 `peak_derivation` 이 구조화된
형태로 들어 있고 `tests/test_hwspec.py` 가 대조한다.

```
예측 실효 피크  165.2 x 2400/2520 = 157.333 TFLOP/s
실측                              156.73     (예측의 99.6 %)
```

#### 눈금 = **32 ns** — 여섯 길이에서 전부 일치

`tools/tick_probe.cu` (spin 커널, 목표 2 us ~ 2 ms 여섯 구간 x 800 회) +
`tools/tick_report.py`.

| 후보 | 설명력 | 홀수 배수 | 판정 |
|---:|---:|---:|---|
| 16 ns | 100 % | **0.0 %** | ⛔ 기각 — 진짜 눈금의 짝수배다 |
| **32 ns** | **100 %** | 38~52 % | ★ 채택 |
| 64 ns 이상 | 55~61 % 이하 | — | 설명력 미달 |

**5090 의 32 → 16 정정을 반복하지 않았다.** 그때는 홀수 배수 검사가 없었다.

```
A6000 1024 ns   /   ★ 4090 32 ns   /   5090 16 ns   /   H100 32 ns
```

#### SOL — 문턱 아래 **0 개**, 최소 여유 592 %

문턱 = 3 x `launch_bracketed_grid`(3.072 us) = **9.216 us**.

| 층 | 형상 | SOL | 문턱 대비 |
|---|---|---:|---:|
| a_workload / c_waves | `256x4096x4096` | 54.60 us | 592 % |
| b_kvary | `4096x4096x256` | 54.60 us | 592 % |
| b_kvary | `8192x4096x128` | 71.39 us | 775 % |

**가장 나쁜 구간(문턱의 70~90 %)에 걸친 형상이 하나도 없다.** 앵커
(`DRIFT_SHAPES` 2048³/4096³)도 함께 봤다 — 둘 다 문턱의 수십 배다.

#### forward-compat — **+1.4~2.1 %**, 브래킷 측정에는 영향 없음

같은 컨테이너에서 `libcuda` 만 바꿔 **교대로** 25 회씩 (compat 610.43.02 vs
호스트 native 580.173.02).

| 항목 | compat | native | 차이 | U (중심 312) |
|---|---:|---:|---:|---:|
| `launch_async_small` | 2.0188 us | 1.9775 us | **+41.3 ns (+2.09 %)** | 533 |
| `launch_async_grid` | 2.0026 | 1.9747 | +27.9 ns (+1.42 %) | 483 |
| `launch_bracketed_small` | 2.3360 | 2.3040 | +32.0 ns (눈금 한 칸) | **308 — 겹침** |
| `launch_bracketed_grid` | 3.0720 | 3.0720 | 0.0 | **312 — 동일** |

```
A6000   +53.5 ns (+3.01 %)   느려진다
★ 4090  +41.3 ns (+2.09 %)   느려진다 (A6000 과 같은 방향·크기)
5090    -167 ns  (-9.1 %)    빨라진다
```

**하네스가 실제로 쓰는 브래킷 측정에서는 차이가 없다** (U 가 중심값이다).
유지한다 — 끄면 유저 모드가 다시 호스트마다 달라진다.

> ### ★ 이 표가 눈금 32 ns 의 두 번째 근거다
>
> 눈금은 이벤트 타이머의 해상도이고 그것을 정하는 것은 **드라이버**다
> (nvcc 가 아니다). 그래서 "compat 으로 쟀는가 native 로 쟀는가" 가 눈금
> 판정의 진짜 축인데, 위 표가 **그 축에서 브래킷 측정이 갈리지 않는다**는
> 것을 보여준다:
>
> ```
> launch_bracketed_grid    3.0720 us 로 양쪽 동일 (U 312 = 중심)
>                          ★ 3.0720 us = 96 x 32 ns — 격자 위다
> launch_bracketed_small   32 ns 차이 = 정확히 눈금 한 칸 (U 308, 겹침)
> ```
>
> 이 캠페인의 눈금은 **이미지 안(compat 610.43.02, nvcc 13.3.73)에서**
> 쟀고 여섯 길이에서 일치했다. 프로브 산출물에 그 조건이 스탬프로 박혀
> 있다 (`driver_api_version=13030 nvcc_version=13.3.73`).

#### 프로브를 어디서 돌렸는가 — 전부 이미지 안

| 프로브 | 컴파일·실행 | 스탬프 |
|---|---|---|
| `peak_mma_probe.cu` | 이미지 안 | `driver_api_version=13030 / nvcc 13.3.73` |
| `tick_probe.cu` | 이미지 안 | 같음 |
| `compat_ab.py` | 이미지 안 (`libcuda` 만 바꿔 A/B) | — |
| `sol_screen.py` | 호스트 (순수 산술, `env.json` 만 읽는다) | 드라이버 무관 |

`tools/probe_env.py` 가 산출물의 스탬프를 `env.json` 과 대조하고, 어긋나거나
스탬프가 없으면 **exit 4** 다. `tick_report.py` 는 표본을 읽기 전에 이 검사를
먼저 한다.

### G-4 — 드리프트 3값 (두 번 측정)

```
drift --gpu <UUID> --max-touch 6000 --step 100
```

| | 1차 | 2차 |
|---|---:|---:|
| 문턱 | 1,498 모듈 | **1,585** |
| 문턱 위 기울기 | 135.47 us/1k | **133.83** |
| 왜곡 배율 | 157.0 배 | 157.8 |
| 터치 실패 | 770/6,000 (12.8 %) | 770/6,000 (12.8 %) |
| 실제 도달 | 5,230 모듈 | 5,230 |

**문턱 흔들림 87 은 `--step`(100) 안이다** — 해상도의 ±1 스텝. 권장
세그먼트 **634** 는 낮은 쪽 문턱(1,498) 기준으로도 42 % 라 안전하다.

```
A6000/13.3    문턱   962   기울기  50.6   세그먼트   384
★ 4090        문턱 1,585   기울기 133.8   세그먼트   634
5090          문턱 4,040   기울기 117.9   세그먼트 1,616
4090(앞선 보고) 문턱 1,394   기울기 158.5   세그먼트   557   <- 데이터 없음
```

문턱이 앞선 보고와 7 % 차이(해상도 안)로 같은 자리에 떨어졌다. 카드·호스트·
드라이버가 전부 다른데도 그렇다. 기울기는 133.8 vs 158.5 로 더 벌어졌다.

> ⛔ `--max-touch` 를 앞선 보고의 1,394 에 맞추지 않은 것이 결정적이었다.
> **터치 실패가 12.8 %** 라 6,000 을 요청해도 5,230 모듈만 쌓인다. 3,000 이면
> 2,600 이라 문턱 위 구간이 1,100 뿐이고 기울기 추정이 훨씬 나빴다.

### ⚠️ `b 변화 −25.9 %` 는 **적합 붕괴**다 — 클럭도 발열도 아니다

도구의 옛 문구는 *"크면 모듈이 아니라 클럭/발열이다"* 였다. A6000 은
−3.42 % 였는데 여기는 −25.9 % 라 게이트를 그냥 넘길 수 없었다.
원시 시간이 답을 준다 (모듈 0 → 5,230):

```
 256³    12.29 ->  567.30 us   +555.01
 512³    19.46 ->  567.30       +547.84     ★ 넷이 566~567 로 수렴한다
1024³    34.82 ->  566.27       +531.45
2048³   125.92 ->  566.38       +440.46
4096³   911.36 -> 1195.01       +283.65     ★ 짧은 쪽의 절반이다
```

**(1) 짧은 넷이 같은 값으로 수렴한다** — 그 지점부터는 커널이 아니라 런치
경로를 재는 것이다. `t = a + b·work` 에서 기울기 정보가 사라지고 선형 적합이
비선형성을 `b` 로 흡수한다. r² 가 **0.99994 → 0.98572** 로 무너지고 다섯 커널이
전부 −20~−28 % 로 같은 모양인 것이 그 증거다.

**(2) 긴 커널의 증가량이 절반이다** — 오버헤드가 **실행과 겹친다.** A6000
문서는 증가량이 프로브마다 "거의 같다"(+40~85 us)고 적었는데, 그것은 모듈
압력이 낮을 때의 이야기다. `a` 가 546 us 가 되면 911 us 짜리 커널은 그 절반을
실행 뒤에 숨긴다.

직접 증거: **SM 2400 / mem 10251 전 구간 고정이고 온도는 오히려 내려갔다**
(55 → 51 °C). 열이면 `b` 가 나빠져야 한다.

`measure_drift.py` 가 이제 r² 와 짧은 프로브 퍼짐으로 판별한다
(`b_drift_is_fit_artifact`). **`a` 는 유효하다** — 마지막 스텝에서 다섯 커널의
`a` 가 533.7~545.7 us 로 2 % 안에 모인다.

---

## 앞선 시도의 값 — **참고용, 데이터 없음**

| | 앞선 시도의 보고 | 이번 실측 |
|---|---|---|
| SM 클럭 고정 | 2400 | **2400** |
| ridge | 159.9 | **159.9** |
| 드리프트 문턱 | 1,394 | **1,498 / 1,585** (해상도 안) |
| 문턱 위 기울기 | 158.5 us/1k | **133.8 us/1k** |
| 눈금 | 32 ns | **32 ns** ★ 일치 |
| `high_outlier_frac` | 8.4 % | 9.4 % (미고정 리허설, 옛 그리드) |

**출처는 앞선 시도의 보고이며 데이터가 남아 있지 않다.** 목표가 아니라 감이다.
클럭과 ridge 가 같은 값에 떨어졌지만, 이번 값은 이 카드의 관측 하한
(2565 MHz)에서 독립적으로 유도한 것이다.

---

## 캠페인 중 관측

(전수 시작 후 여기에 덧붙인다)
