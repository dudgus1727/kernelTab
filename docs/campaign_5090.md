# RTX 5090 캠페인 — 확정 조건 (2026-08-29 전수 시작)

**24 시간 뒤에 이 문서를 보고 "무엇이 확정이었나" 를 확인한다.**
측정 중에는 이 문서를 고치지 않는다. 관측한 것은 아래 "캠페인 중 관측" 에만 덧붙인다.

---

## 조건

| | 값 | 근거 |
|---|---|---|
| 이미지 | `kerneltab:cu133-24d30fc9` | `docker/build.sh` (이미지 안에서 태그 계산) |
| 커밋 | `2cba82f` 시점 (worktree 깨끗) | |
| **`env_hash`** | **`5bb6f403`** | 재개 키·격리 경계 (구 정의, D-4) |
| **`env_hash_v2`** | **`a13540bb`** | ★ 조건 식별자. 분석·비교는 이것으로 |
| GPU | RTX 5090 `GPU-bd44ffab-3acb-7a93-97b0-5f829d3827d0` (호스트 #1) | sm_120, SM 170, L2 96 MB, VRAM 32 GB |
| 커널 모드 드라이버 | 590.48.01 (호스트, CUDA 13.1) | |
| 유저 모드 libcuda | **610.43.02** (이미지 compat) — A6000 과 동일 | `env_hash_v2` 의 키 |
| nvcc / CUTLASS | 13.3.73 / `dcf215af` (v4.7.0) | |
| **SM 클럭** | 요청 2400 → **실측 2392** (이산 스냅) | `rehearse` 실부하 검증 |
| **메모리 클럭** | 요청 14001 → **실측 13801** (98.6 %) | |
| 실효 피크 | **208.194 TFLOP/s** (209.5 × 2392/2407) | `mma.sync` 로 209.5 확정 |
| 실효 대역폭 | **1766.53 GB/s** (512 bit × 2 × 13801) | |
| **ridge point** | **117.9** FLOP/byte | A6000 159.1 |
| 형상 | **66** | 층 B M 상향, 층 E 2048~16384 |
| 커널 | 열거 7,230 → 빌드 **7,210** → 런치 가능 **6,305** | 8.0 GB |
| `split_k` 축 | **10 종** `[1,2,3,4,5,6,7,8,12,16]` | 5, 7 은 축 덮개가 찾은 구멍 |
| **`segments.kernels`** | **1,616** (문턱 4,040 의 40 %) | 4 세그먼트 / 재시작 32 회 |
| 전체 작업 | **1,262,825** | A6000 980,915 의 1.287 배 |
| 드리프트 감시 | **2048³** (문턱의 6.7 배, 민감도 +344 %) + 4096³ 참고 | |
| `launch_overhead` | 4.096 us → `below_launch_overhead` 문턱 **12.288 us** | |
| 노이즈 계수 | `sigma_abs 8e-06 / sigma_rel 0.000761 / tick 1.6e-05` | 전수 앵커 1,152행에서 재측정 (D-6 수정 후) |
| 전수 예상 | **~24 h**, 재시작 오버헤드 1.1~1.6 % | |

### 실행 명령 — 두 플래그를 빼지 마라

```bash
docker run --rm -e NVIDIA_DISABLE_REQUIRE=1 \
    --gpus '"device=GPU-bd44ffab-3acb-7a93-97b0-5f829d3827d0"' \
    -v $PWD/data:/data kerneltab:cu133-24d30fc9 sweep
```

* `-e NVIDIA_DISABLE_REQUIRE=1` — 호스트 CUDA(13.1) < 이미지(13.3). `container.md` 8 절
* **`--user` 를 주지 마라** — rootless 에서는 오히려 볼륨에 못 쓴다. `container.md` 9 절

---

## 게이트 통과 기록

| 게이트 | 결과 |
|---|---|
| G-0 이미지·환경 | 통과. CUTLASS 2.x 가 sm_120 에서 빌드 99.7 % + 참조 대조 12/12 |
| G-0b 축 덮개 | **exit 0** (공간 밖 2.8 %, 전부 근거 있는 `stages=1`) |
| G-1/G-2 클럭 | 통과. 2392/13801, 최대 이탈 −0.29 % (지원 클럭 한 칸) |
| G-3 실효 피크·ridge | 208.194 / 1766.53 / 117.9 — 예측과 일치 |
| **G-4 드리프트** | 문턱 **4,040** (3 회 측정 폭 0), 기울기 107~118 us/1k, 왜곡 94~111 배 |
| G-5 세그먼트 | 1,616. 재시작 오버헤드 1.1~1.6 % |
| G-6 리허설 | 정합성 전부 통과 (`smem`/`hmma`/`regs`/occupancy 불일치 0/7210) |
| **G-7 재현성** | **5/5 통과** |

## ⚠️ 캠페인 중 알려진 것

* **`high_outlier_frac` 이 A6000 의 2 배다** (24~26 % vs 10.65 %). 품질 표시이지
  결측이 아니다 (C-2). 소비 쪽이 `status == "ok"` 로 거르면 A6000 보다 2 배 더
  버린다.
* **앵커 형상 변경 경계** — `env_hash` 가 안 바뀌므로 여기 적는다.
  ```
  512³  감시 구간   ~ 2026-08-28T18:55:25Z   측정 행 40,324
  2048³ 감시 구간   2026-08-29T02:06:11Z ~
  ```
  옛 앵커는 `results/anchors.drift512.jsonl` 로 분리했다. **일반 측정값은
  영향받지 않는다** — 감시 프로브는 판정만 하고 측정 대상이 아니다. 다만 512³
  구간에서 드리프트 판정이 덜 민감했을 가능성이 있어 구간을 남긴다
  (512³ 은 +1750 % 로 오히려 민감했으므로 위험은 낮다).
* **D-6 미해결** — 번들이 A6000 노이즈 계수를 싣는다. **번들 만들기 전에 고칠 것.**

## 캠페인 중 관측 (2026-08-30 완료 후 기록)

```
2026-08-29T03:17:36Z ~ 2026-08-30T01:47:44Z    22.5 h
라운드 8 x 세그먼트 4 = 32 회 재시작, 전부 정상 완료
측정 1,193,933 행 (게이트 단계 73,380 행 포함 총 1,267,313)
처리량 53,058 행/h = 67.9 ms/job  ★ 22.5 시간 내내 변동 없음
```

### A6000 과 나란히

| | A6000 (`828baa64`) | **RTX 5090 (`5bb6f403`)** |
|---|---:|---:|
| 벽시계 | 15.3 h | **22.5 h** |
| 작업 수 | 980,915 | **1,262,825** (1.287 배) |
| 작업당 | 56.2 ms | **67.9 ms** |
| 앵커 절대값 이동 | 0.07 % | **0.00 ~ 0.51 %** |
| 드리프트 경고 | — | **2 건 (오경보 0)** |
| `high_outlier_frac` | 10.65 % | **22.21 %** |
| 실패 status | — | **0 건** |

시간이 늘어난 것은 GPU 가 느려서가 아니라 **작업 수가 1.287 배**이기
때문이다 (`split_k` 축 8 -> 10). 시간 예산 프로토콜에서는 GPU 속도가
캠페인 길이를 거의 바꾸지 않는다 (`decisions.md` 20).

### 드리프트 — 22.5 시간 동안 추세 없음

```
2048³ 감시 프로브 153 회 점검
  110.59 ~ 114.69 us,  고유값 6 개,  변동폭 3.70 %
  첫 3 시간 중앙 [112.6, 110.6, 110.6]
  마지막 3 시간   [110.6, 112.6, 110.6]     ★ 단조 증가 없음
참고 4096³ 변동폭 0.28 %
```

두 이산값(110.6 / 112.6 us)을 오갈 뿐이다. **세그먼트 1,616 이 문턱
4,040 의 40 % 라는 설계가 그대로 들었다.**

### 앵커 — 세그먼트/라운드에 균등하게, 이동 없음

```
768 줄,  세그먼트 0~3 각 192,  라운드 0~7 각 96
짧은 앵커(2048³) 6 개 중 ★ 5 개가 64 회 측정에서 변동폭 0.00 %
긴 앵커(4096³)  첫->끝 이동 -0.28 ~ +0.51 %
```

### 감시 발화 — 오경보 0

```
[tel] 정상 기록   134 회 (20 시간 시점)   <- 옛 코드였다면 전부 오경보
!!   실제 경고     2 건
```

`sw_power_cap` 판정을 클럭 분포로 바꾼 것(`decisions.md` 22)이 정확히 이
자리다. 고치지 않았으면 **600 초 주기 x 22.5 시간 = 135 회가 전부 오경보**
였고, 진짜 경고 2 건이 그 속에 묻혔다.

### 깊은 클럭 딥 — 7 회, 41 행 (0.0032 %)

| 시각 (UTC) | 최저 | 행 | 온도 | 전력 |
|---|---:|---:|---:|---:|
| 08-29 06:09:58 | 2370 | 7 | 58 ℃ | 316 W |
| 08-29 07:42:27 | 2235 | 2 | 62 ℃ | 353 W |
| 08-29 07:52:50 | 2370 | 8 | 59 ℃ | 392 W |
| 08-29 08:21:12 | 2212 | 1 | 60 ℃ | 337 W |
| 08-29 20:28:02 | 2235 | 6 | 63 ℃ | 324 W |
| 08-29 22:17:29 | **2055** | 8 | 59 ℃ | 334 W |
| 08-29 23:45:07 | 2107 | 9 | 61 ℃ | 339 W |

전부 1~2 초 순간 이벤트다. **전력(316~392 W, 상한 600 W)도 온도(58~63 ℃)도
원인이 아니다** — 지속 없이 튀었다 돌아온다. 원인은 규명하지 못했다.

측정 줄마다 `sm_clock_mhz` 가 기록되므로 소비 쪽이 `df[df.sm_clock_mhz >=
2377]` 로 걸러낼 수 있다 (`consumer_contract.md` 11 절).

> 나머지 편차는 지원 클럭 한 칸(2385 MHz, −0.29 %)이고 전체의 20.8 % 다.
> 부스트 알고리즘의 입도라 제거할 수 없다.

### 다음 캠페인의 판단 기준

이 값들이 "이 정도면 정상인가" 의 기준이 된다.

```
드리프트 감시 변동폭   3.70 % 이내, 단조 증가 없음
짧은 앵커              대부분 변동폭 0.00 %
깊은 클럭 딥           0.005 % 이하
감시 오경보            0 건 (플래그가 아니라 클럭 분포로 판정할 것)
처리량                 전 구간 일정 (편차 없음)
```

### ⚠️ 게이트를 통과하고도 캠페인 **후에** 둘이 더 나왔다

```
validate_table 의 ok 게이트     빠른 GPU 일수록 표가 나쁘다고 판정했다
RELEASE.md 의 A6000 스펙 하드코딩  "스펙을 쓰지 마라" 가 틀린 스펙을 실었다
```

게이트 9 개를 통과했다고 끝이 아니다. **산출물을 실제로 만들어 봐야
드러나는 것이 있다.**

