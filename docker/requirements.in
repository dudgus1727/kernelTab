# kerneltab 런타임 의존성 (측정 + export + 번들 전부 포함)
#
# pandas 는 **필요하다.** 이전 주석에 "코드 어디서도 쓰지 않는다" 고 적혀
# 있었으나 사실이 아니다 — core/table.py 의 로더가 to_pandas() 를 쓰고
# core/bundle.py 도 쓴다. 둘 다 정답 누출을 막는 소비 경로의 핵심이고,
# 없으면 그것을 검증하는 테스트 41개가 통째로 스킵된다 (R-1).
nvidia-ml-py==13.610.43
pyarrow==25.0.1
pandas==2.3.3

# pytest 는 **이미지에 넣는다.** 컨테이너에서 테스트를 돌리는 것이
# Python 3.10 실검증의 핵심이기 때문이다 (migration 10). ctypes 구조체 정렬
# 같은 것은 정적 분석으로 잡히지 않는다.
pytest==8.4.2
tomli==2.3.0 ; python_version < "3.11"
